import 'package:flutter/widgets.dart';

class TodoEmptyList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('The todo list is empty right now!'),
    );
  }
}
